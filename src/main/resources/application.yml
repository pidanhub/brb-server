server:
  port: 18950

spring:
  mail:
    host: smtp.qq.com
    username: 2476594519@qq.com
    password: ivccswxyrtxvecje
    default-encoding: UTF-8
    protocol: smtp
    properties:
      mail:
        smtp:
          socketFactoryClass: javax.net.ssl.SSLSocketFactory
          #表示开启 DEBUG 模式，这样，邮件发送过程的日志会在控制台打印出来，方便排查错误
          debug: true
          ssl: true
  datasource:
    url: jdbc:mysql://101.43.242.216:3306/brbdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: "012345"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  mvc:
    static-path-pattern: /images/**
  web:
    resources:
      static-locations: file:/images
  redis:
    host: 127.0.0.1
    port: 6379
    password: zzsroot666! #上传取消密码
    timeout: 10000
  servlet:
    multipart:
      max-request-size: 20MB
      max-file-size: 20MB

mybatis:
  type-aliases-package: com.save.brbserver.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: kjFBjnlndnlanlf

logging:
  file:
    name: ./logspring-brb.log
  level:
    root: info #日志级别 root表示所有包，也可以单独配置具体包 fatal>error>warn>info>debug>trace>off

#swagger:
#  enabled: true